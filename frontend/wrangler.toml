name = "insta-save"
compatibility_date = "2024-12-01"

# Cloudflare Pages will auto-detect the build output in dist/
# Build command: npm run build
# Output directory: dist

# R2 bucket binding — set your bucket name here after creating it
# in the Cloudflare dashboard: Workers & Pages > R2
[[r2_buckets]]
binding = "R2_BUCKET"
bucket_name = "insta-save"

# Set your R2 public URL as an environment variable.
# Enable public access on your R2 bucket in the Cloudflare dashboard,
# then paste the public URL here.
[vars]
R2_PUBLIC_URL = "https://pub-764d6cc304ed471c8b41792819cf420f.r2.dev"
